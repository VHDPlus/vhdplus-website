---
title: Get Started with VHDP
sidebar_label: Setup Software
description: Start using VHDP quickly. The FPGA Revolution is waiting for you
keywords:
    - vhdp
    - vhdplus
    - getstarted
    - fpga
image: /img/logo.PNG
id: getstarted
---

import Highlight from '@theme/CodeBlock'
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

export const IDEVersion = '0.9.9.1';

export const Win64DownloadName = 'VHDPlus-' + IDEVersion + '-x64.msi'
export const Win64DownloadS = 'https://cdn.vhdplus.com/vhdpluside/' + IDEVersion + '/' + Win64DownloadName
export const Win64Download = <a href={Win64DownloadS}>{Win64DownloadName}</a>

export const DEB64DownloadName = 'VHDPlus-' + IDEVersion + '-x64.deb'
export const DEB64DownloadS = 'https://cdn.vhdplus.com/vhdpluside/' + IDEVersion + '/' + DEB64DownloadName
export const DEB64Download = <a href={DEB64DownloadS}>{DEB64DownloadName}</a>
export const DEB64PackageManager = 'wget ' + DEB64DownloadS + '; ' + 'sudo dpkg -i ' + DEB64DownloadName

export const RPM64DownloadName = 'VHDPlus-' + IDEVersion + '-x64.rpm'
export const RPM64DownloadS = 'https://cdn.vhdplus.com/vhdpluside/' + IDEVersion + '/' + RPM64DownloadName
export const RPM64Download = <a href={RPM64DownloadS}>{RPM64DownloadName}</a>
export const RPM64PackageManager = 'sudo rpm –i ' + RPM64DownloadS

export const TAR64DownloadName = 'VHDPlus-' + IDEVersion + '-x64.tar.gz'
export const TAR64DownloadS = 'https://cdn.vhdplus.com/vhdpluside/' + IDEVersion + '/' + TAR64DownloadName
export const TAR64Download = <a href={TAR64DownloadS}>{TAR64DownloadName}</a>

export const Button = ({children, href}) => (
  <span
    style={{
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
      backgroundColor: '#0091db',
    }}>
    <a href={href}
        style={{
            color: '#fff',
            padding: 4,
            fontSize: 16,
        }}>
    {children}
    </a>
  </span>
);

## Software Setup

Use the benefits of code suggestions and vendor-independent libraries by using VHDPlus IDE, the easiest way to program your FPGA.

<div class="fluidMedia"><iframe id="ytplayer" type="text/html" width="100%" src="https://www.youtube.com/embed/BlZS3-MZHCI?autoplay=0&origin=http://vhdplus.com" frameborder="0" allowfullscreen></iframe></div>

**Important Notes:**
> :warning: At this point the VHDP language has not left WIP state. Breaking changes might occur until version 1.0.**

### Install VHDPlus IDE

<Tabs
  groupId="operating-systems"
  defaultValue="windows"
  values={[
    {label: 'Windows', value: 'windows'},
    {label: 'Linux', value: 'linux'},
  ]}>
  <TabItem value="windows">

1. Download <a href="https://download.altera.com/akdlm/software/acdsinst/18.1std/625/ib_installers/max10-18.1.0.625.qdz" target="_blank">MAX 10 device support</a>¹
2. Download and install <a href="https://download.altera.com/akdlm/software/acdsinst/18.1std/625/ib_installers/QuartusLiteSetup-18.1.0.625-windows.exe" target="_blank">Quartus Prime Lite for Windows</a>²
3. Download and install VHDPlus IDE

<Button href={Win64DownloadS} children={Win64DownloadName}></Button>

  </TabItem>
  <TabItem value="linux">

1. Download <a href="https://download.altera.com/akdlm/software/acdsinst/18.1std/625/ib_installers/max10-18.1.0.625.qdz" target="_blank">MAX 10 device support</a>¹
2. Download and install <a href="https://download.altera.com/akdlm/software/acdsinst/18.1std/625/ib_installers/QuartusLiteSetup-18.1.0.625-linux.run" target="_blank">Quartus Prime Lite for Linux</a>²
3. Install VHDPlus IDE for your Linux distro:

<Tabs
  defaultValue="debian"
  values={[
    {label: 'Debian Based Linux', value: 'debian'},
    {label: 'RPM Based Linux', value: 'rpm'},
    {label: 'Other Linux', value: 'otherlinux'},
  ]}>
  <TabItem value="debian">

- Using the Terminal:

<Highlight language="Bash">{DEB64PackageManager}</Highlight>

- Alternatively download and install <span children={DEB64Download}></span> manually


  </TabItem>
  <TabItem value="rpm">

- Using the Terminal:

<Highlight language="bash">{RPM64PackageManager}</Highlight>

- Alternatively download and install <span children={RPM64Download}></span> manually


  </TabItem>
  <TabItem value="otherlinux">

1. Download <span children={TAR64Download}></span>
2. Unpack the contents of the `tar.gz` to any location and execute the `VHDPlus` executable to start the app.


  </TabItem>
</Tabs>

  </TabItem>
</Tabs>




¹MAX 10 device support:<br></br>
The VHDPlus Core is equipped with a MAX 10 FPGA. Select the device family depending on your development board.

²Quartus Prime: <br></br>
Quartus Prime is necessary for compiling your code and programming your FPGA.

### Drivers

You need to install the drivers according to your programmer at hand in order to program your FPGA. Our VHDPlus Core features an ARROW USB Programmer because of its additional USB to UART interface.

Follow **[this guide](/docs/getstarted/drivers)** to download and install the driver for the **Arrow USB Programmer**.

### Connect VHDPlus IDE with Quartus

For direct compiling and programming of VHDPlus IDE a connection via Quartus is necessary. 
If Quartus is installed in default directory no further steps are needed. 
 Otherwise an adjustment of the Quartus' path can be realized by opening the VHDPlus IDE and navigating through`Extras -> Options -> General`

![Select Quartus Path](/img/getstarted/QuartusPath.png)

When Quartus is detected successfully the boundary turns green.

### Simulation with GHDL

<Tabs
  groupId="operating-systems"
  defaultValue="windows"
  values={[
    {label: 'Windows', value: 'windows'},
    {label: 'Linux', value: 'linux'},
  ]}>
  <TabItem value="windows">

#### Using the new Package Manager (recommended)
1. Open the Package Manager `Extras` -> `Package Manager`
2. Install `GHDL` and `GTKWave` and wait for them to finish

#### Manual installation
1. Download and Install <a href="http://ghdl.free.fr/ghdl-installer-0.29.1.exe" target="_blank">GHDL</a>
2. Download an extract <a href="https://sourceforge.net/projects/gtkwave/files/gtkwave-3.3.100-bin-win64/gtkwave-3.3.100-bin-win64.zip/download" target="_blank">GTK Wave</a>
3. Specify the path for GHDL and GTKWave executables in `Extras` -> `Settings` -> `Simulator`


  </TabItem>
  <TabItem value="linux">

1. Download and Install GHDL with `sudo apt-get install ghdl` or follow <a href="http://ghdl.free.fr/site/pmwiki.php?n=Main.Installation" target="_blank">these</a> instructions
2. Download an install GTKWave with `sudo apt-get install gtkwave` or follow <a href="http://gtkwave.sourceforge.net/" target="_blank">these</a> instructions
3. Specify the path for GHDL and GTKWave executables in `Extras` -> `Settings` -> `Simulator`


  </TabItem>
</Tabs>

Learn more about simulation with GHDL [here](/docs/getstarted/ghdl)

### Simulation with ModelSim

1. Download and install <a href="https://download.altera.com/akdlm/software/acdsinst/18.1std/625/ib_installers/ModelSimSetup-18.1.0.625-windows.exe" target="_blank">ModelSim</a> or <a href="http://download.altera.com/akdlm/software/acdsinst/18.1std/625/ib_installers/ModelSimSetup-18.1.0.625-linux.run" target="_blank">ModelSim for Linux</a>
2. Specify the path for the modelsim_ase folder in `Extras` -> `Settings` -> `Simulator`
3. Run the simulation in the GHDP viewer or with a right click on a VHDL file

Learn more about simulation with ModelSim [here](/docs/getstarted/modelsim)

## Program with VHDP
[**Click here**](/docs/getstarted/vhdp) to see the full documentation of the language VHDP. You have the choise between a written documentation and a video series.

Here is a first tutorial on how to create a simple Blink program:

<div class="fluidMedia"><iframe id="ytplayer" type="text/html" width="100%" src="https://www.youtube.com/embed/RAWTzf6VDLM?autoplay=0&origin=http://vhdplus.com" allowFullScreen></iframe></div>

### Create your first program:

> MAX10 Device Support, Quartus and the Arrow Programmer Driver are required for this.

1. Start VHDPlus IDE and create a new project (top left).
2. Inside the vhdp file you can write your code. This is a simple blink example:
```vhdp
/*
Blink example
*/

Main
(
    --I/Os of the FPGA
    LED : OUT STD_LOGIC := '0';  --STD_LOGIC contains states like '1', '0' or 'Z' for High Impedance
)     
{   
    Process Demo ()
    {
        Thread --Every Thread is an endless loop
        {
            LED <= '1';
            Wait(250ms);
            LED <= '0';
            Wait(250ms);
        }
    }
}
```
3. To test your code click on create (grey hammer). 
For the example code with Core MAX10: 
- Click on the red LED in the right drawing of the Core MAX10
- Click on "LED" in the "Unconnected" list and hit Enter
- Do the same for CLK that is located right next to the FPGA chip
- Click on Compile
4. Compiling can last up to several minutes, because the compiler attempts to find the most efficient way to connect the logic elements. 
To program the FPGA, connect the FPGA with an USB cable and click on Program (blue arrow). To save the program on the FPGA, select Extras/Long-Term Programming.

## Comparison between Arduino (C++), VHDP and VHDL

If you are familiar with software or hardware programming, check out this comparison [here](/docs/getstarted/comparison).

<div class="fluidMedia"><iframe id="ytplayer" type="text/html" width="100%" src="https://www.youtube.com/embed/WZTix6MlBNM?autoplay=0&origin=http://vhdplus.com" frameborder="0" allowfullscreen></iframe></div>

## Use other HDL files and IP Cores

If you are not new to FPGAs and want to profit from VHDP while still using your old code and IP Cores, check out [this tutorial](/docs/ide/tools/importconverter).
(If you don't have a VHDP file with a Main(), the Verilog or VHDL file with the name of the project is the top entity)

<div class="fluidMedia"><iframe id="ytplayer" type="text/html" width="100%" src="https://www.youtube.com/embed/maYOYYNtw2U?autoplay=0&origin=http://vhdplus.com" allowFullScreen></iframe></div>

## Community

Check out what other people have done with VHDP. Here is a list of tutorials from us:

1. [Smart route following robot](/docs/community/motor)
2. [Camera with Object Detection](/docs/community/camera)
3. [LED Matrix](/docs/community/matrix)
4. [Spectrum Analyzer](/docs/community/spectrum)

You can find much more examples [here](https://github.com/search?utf8=%E2%9C%93&q=vhdplus).
